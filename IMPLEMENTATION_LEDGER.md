# HYMN Production Hardening Implementation Ledger

This ledger tracks the acceptance criteria in `HYMN Production Hardening Mandate.pdf`. A phase is marked completed only when its repository-verifiable criteria pass. Provider and deployment checks remain blocked until suitable staging infrastructure and credentials are available.

| Phase | Requirement | Status | Files / migration | Tests and verification | External dependency / remaining risk |
|---|---|---|---|---|---|
| 0 | Repository audit and implementation ledger | completed | `docs/production-readiness-audit.md`, this file | Route, model, auth, payment, distribution, royalty, payout and risk inventories reviewed; baseline commands recorded in audit | Live providers and production-like database require staging |
| 1 | Remove fabricated analytics | completed | `lib/analytics.ts`, `lib/db.ts`, `lib/types.ts`, `components/analytics-overview.tsx`, `scripts/verify-analytics-domain.ts`; `20260726120000_verified_analytics_provenance` | `npm run test:analytics`; TypeScript; touched-file ESLint. Empty and persisted-zero regressions pass | Provider import must explicitly mark rows verified and record provenance |
| 2 | Safe migrations and deployment | completed | `package.json`, `prisma/fresh-baseline.sql`, `scripts/migrate-fresh-database.ts`, `scripts/verify-migration-safety.ts`, `docs/database-deployment.md`; `20260726125000_phase2_integrity_indexes` | Guarded fresh install passed on disposable PostgreSQL with all 39 migrations current; the regenerated baseline includes the latest tables plus raw partial-index and immutable-audit invariants; Prisma validate/generate, migration safety, TypeScript, lint and diff check passed | Production operators must still take and verify an environment-specific backup before deployment; no destructive automatic acceptance is used |
| 3 | PostgreSQL/Prisma canonical source | completed | `lib/prisma.ts`, `lib/db.ts`, `lib/distribution-db.ts`, `lib/spotify-auth-store.ts`, `scripts/backfill-legacy-spotify.ts`, `scripts/verify-postgres-canonical.ts`; `20260726130000_spotify_prisma_canonical`, `20260726131000_postgres_identity_canonical`, `20260726132000_prisma_checkout_orders` | Disposable PostgreSQL integration passed identity/password/referral persistence, real beat listing, subscriptions, relational checkout, idempotent completion, customer/admin release consistency, ISRC track lookup, real queue counts and persisted audit; production rejects MySQL/memory writes | Explicit legacy adapters remain development-only; lower-priority unmigrated legacy features fail clearly rather than silently persisting to memory in production; Spotify import remains dry-run by default |
| 4 | Razorpay webhook architecture | active | `lib/razorpay.ts`, `lib/payment-webhooks.ts`, `lib/payment-reconciliation.ts`, webhook/admin replay APIs; `20260726140000_razorpay_webhook_events` through `20260726143000_purchase_order_item_identity` | Raw-body constant-time signature verification, concurrency-safe durable dedupe, shared browser/webhook/reconciliation fulfilment, transactional checkout/distribution/subscription effects, exact purchase identity, idempotent notifications, failure/refund/dispute reversal and the complete local event-order/replay matrix pass | Repository criteria pass and test credentials plus a distinct webhook secret are configured only in ignored local environment state; live Razorpay delivery/replay remains unverified until a reachable callback is registered in the Razorpay test dashboard |
| 5 | Secure file access | active | `lib/private-storage.ts`, authenticated asset APIs, private release uploader, private beat licences/deliverables, `scripts/verify-private-storage*.ts`; `20260726150000_private_stored_assets` through `20260726152000_private_beat_deliverables` | Checksums, allowlists, MIME/magic/size/path validation, image dimension parsing, audio signatures, owner/admin/purchaser authorization, revoked-access denial, no-store/noindex delivery, concurrent exactly-once private licence generation, and public-URL rejection pass locally | Durable production object storage and deployment-level expiry/retention verification remain external; local adapter deliberately fails on ephemeral Vercel |
| 6 | Canonical release-status system | completed | `lib/release-status-engine.ts`, `lib/distribution-db.ts`, status cron, `scripts/verify-release-status*.ts`; `20260726160000_release_status_state_machine`, `20260726161000_release_transition_metadata` | Canonical states/transitions, reasoned manual overrides, versioned immutable history, queue/status unification, no date-only live transition, and a real PostgreSQL two-writer race pass; exactly one concurrent transition commits | Provider confirmation remains required before `live`; external provider testing is tracked in Phase 7 rather than weakening the status invariant |
| 7 | Harden Direnote | active | `lib/direnote/**`, `lib/distribution-service.ts`, `lib/distribution-idempotency.ts`, `scripts/verify-direnote-integration.ts`; `20260726170000_direnote_idempotency` | Deterministic payload identity, concurrent pre-call claim, retry takeover/count, submitted dedupe, redacted evidence, configurable abort timeout, HTTP rejection parsing, and durable distributor release ID/UPC/ISRC persistence pass with local transport and PostgreSQL fixtures | Repository contract behavior passes; the exact live DireNote sandbox contract cannot be claimed without provider credentials and an authoritative current response contract |
| 8 | Corrections, updates and takedowns | completed | `lib/release-change-requests.ts`, user/admin APIs and timelines, `components/release-change-request-queue.tsx`, `scripts/verify-release-change-requests-integration.ts`; `20260726180000_release_change_requests`, `20260726181000_release_change_request_history` | Ownership, duplicate-active prevention, legal workflow transitions, append-only event history, user/admin UI, role-specific review permission, required notes/references, audit, and canonical takedown transitions pass on PostgreSQL | Actual store execution remains explicitly manual where a provider API is unsupported; no external completion is claimed without a recorded partner reference |
| 9 | Royalty, wallet and reconciliation | completed | `lib/royalty-import.ts`, private import and unmatched-resolution APIs/UI, `lib/financial-reconciliation.ts`, `scripts/verify-royalty-ledger-integration.ts`; `20260726190000_royalty_statement_ledger` through `20260726193000_unmatched_resolution_line` | Preview/confirmation, private originals, serializable all-or-nothing import, checksum/source-line dedupe, 6-decimal precision, complete owner/split allocations, deterministic wallet entries, negative adjustments, manual matching provenance, rollback, simultaneous duplicate-import rejection, and reconciliation gates pass on PostgreSQL | Provider statement column mappings remain configured per actual provider format; no unverified revenue is synthesized |
| 10 | Manual KYC and payout confirmation | completed | `lib/payout/credentials.ts`, `lib/payout.ts`, user/admin KYC and payout UIs/APIs, private payout-proof and owner receipt routes, `scripts/verify-payout-kyc-integration.ts`; `20260726200000_manual_kyc_payout_confirmation` through `20260726202000_payout_receipts_timeline` | Encrypted KYC, reservation concurrency, legal transitions, UTR confirmation, private proof linking, downloadable paid receipt, five-step artist/admin timeline, fee/debit ledger and failed-fund release pass on PostgreSQL | Actual bank transfer execution remains manual and must never be marked paid without real payment evidence/reference |
| 11 | Admin roles and immutable audit | completed | `lib/access.ts`, `lib/audit-log.ts`, `lib/rate-limit.ts`, audit viewer/export, permission-mapped admin APIs, session/login controls; `20260726210000_admin_rbac` through `20260726213000_security_rate_limits` | No route calls the broad admin guard; role matrix, recent authentication, two-person adjustments, structured actor/request/network/risk fields, login/session audit, filters/cursor/CSV export, persistent rate limits and database-enforced append-only rows pass | IP values depend on a trusted deployment proxy configuration; production role assignments still require operator governance |
| 12 | Managed CRBT/OAC/Content ID requests | completed | `lib/managed-services.ts`, user/admin APIs and dedicated portals; `20260726220000_managed_service_requests` through `20260726222000_managed_service_details` | Detailed service-specific declarations, CRBT track/provider/start-point fields, OAC channel evidence, Content-ID conflict/risk flags, private documents, ownership, deterministic idempotency, pagination, request envelopes/IDs, rate limiting, information-required/rejection/manual/provider-status/completion paths pass on PostgreSQL | Every service is honestly labelled manual; partner availability, approval and turnaround remain external and are never inferred from submission |
| 13 | Full regression/security verification | active | `next.config.ts`, aggregate and PostgreSQL integration commands, regenerated `prisma/fresh-baseline.sql` | All domain suites and ten PostgreSQL integration suites pass on a guarded 39-migration baseline; TypeScript, zero-error lint, 142-route production build, supplied-credential scan and diff check pass; production CSP removes `unsafe-eval` and narrows Razorpay origins | Current online npm registry audit requires explicit approval to transmit dependency metadata; offline cache reported zero but is not treated as authoritative. Browser accessibility/load/security scans, live payment webhook delivery, durable object storage and provider sandboxes remain environment-dependent |

## Verification history

- 2026-07-26: `npm run test:analytics` passed; `npx tsc --noEmit` passed; touched-file ESLint completed with zero errors and one pre-existing warning in `lib/db.ts`.
- 2026-07-26: Prisma commands initially failed because the sandbox could not access the platform cache. Escalated validation then correctly failed closed without `DATABASE_URL`; generation also encountered a locked Windows query-engine binary. These are environment/process blockers, not recorded as passing.
- 2026-07-26: confirmed `build` is `prisma generate && next build` and contains no schema mutation.
- 2026-07-26: `npx prisma validate`, `npx tsc --noEmit`, all seven domain verification scripts and a 132-route production build passed. ESLint reported zero errors (warnings remain). `git diff --check` passed.
- 2026-07-26: exact-value scan confirmed the supplied Razorpay test key and secret were not written to the repository. No live Razorpay claim is recorded because a webhook secret and reachable staging callback are not configured.
- 2026-07-26: aggregate `npm test` passed all seven domain suites; `npx tsc --noEmit`, focused configuration ESLint, and `git diff --check` also passed after adding production security headers and removing the `db:push` shortcut.
- 2026-07-26: Phase 2 fresh rehearsal passed against disposable PostgreSQL 18: guarded baseline installation recorded all 20 migrations and `prisma migrate status` reported current. Upgrade rehearsal generated the committed pre-hardening schema, marked its eight historical migrations, applied all 12 hardening migrations with `prisma migrate deploy`, and reported current. The workspace-local database runtime was stopped and removed afterward.
- 2026-07-26: Phase 3 canonical PostgreSQL integration passed on a clean 22-migration database: password identity, referral relation, beat catalog without demo rows, subscription activation, relational checkout, duplicate payment completion, release/track consistency, queue counts and audit persistence. Legacy MySQL is explicitly disabled in production.
- 2026-07-26: Phase 4 local Razorpay integration passed webhook-before-browser, browser-before-webhook, duplicate/concurrent delivery, amount/currency mismatch, missing-order replay, transactional rollback, full refund and duplicate refund, payment failure, subscription entitlement, and authorized-to-captured cases. Live staging is still unverified without a webhook secret/callback.
- 2026-07-26: Phase 5 storage validation and disposable-PostgreSQL integration passed content validation, owner and purchased-deliverable authorization, refund/revocation denial, and concurrent exactly-once private licence creation. Public beat/release deliverable URLs are no longer emitted by the migrated flows.
- 2026-07-26: Phase 6 unit and disposable-PostgreSQL integration passed transition legality, required reasons, reasoned manual override evidence, scheduled-date behavior, and optimistic concurrency; two simultaneous writers produced one committed transition and one concurrency rejection.
- 2026-07-26: Phase 7 local transport and PostgreSQL integration passed abort timeout, API rejection, concurrent identical claims, retry attempt increment, post-success dedupe, and durable DireNote release ID/UPC/track ISRC persistence. Provider sandbox verification remains external.
- 2026-07-26: Phase 8 PostgreSQL workflow passed duplicate suppression, illegal transition rejection, append-only user/admin timeline, mandatory partner references, metadata completion, and the full `LIVE -> TAKEDOWN_REQUESTED -> TAKEDOWN_PROCESSING -> TAKEN_DOWN` sequence.
- 2026-07-26: Phase 11 PostgreSQL controls passed authentication-freshness boundaries, least-privilege role separation, idempotent two-person wallet adjustments, self-approval denial, concurrent decision exactly-once behavior, non-negative balance enforcement, and database rejection of audit updates/deletes.
- 2026-07-26: Phase 12 PostgreSQL workflow passed release/document ownership, private evidence linking, duplicate suppression, illegal transition denial, required partner references, ordered manual processing through completion, and append-only audit evidence. No provider completion was simulated as a live external result.
- 2026-07-26: Phase 13 caught and corrected a stale fresh baseline, then reinstalled it on empty disposable PostgreSQL with all 35 migrations recorded and reran ten integration suites successfully. Aggregate domain tests, TypeScript, ESLint (zero errors, 94 existing warnings), 141-route production build, supplied-credential scan, and patch whitespace check passed. An offline npm audit reported zero; authoritative online audit remains unclaimed because registry submission was not authorized.
- 2026-07-26: Phase 9 PostgreSQL integration passed serializable atomic import/rollback, six-decimal positive and negative entries, adjustment evidence, unmatched manual resolution, 60/40 split completeness, deterministic wallet credits, and concurrent checksum dedupe without double credit.
- 2026-07-26: Phase 10 PostgreSQL integration passed encrypted/masked KYC submission, pre-verification payout denial, verified held-earning release, concurrent reservation with one winner, required state progression, exact net payment plus fee ledger, duplicate UTR rollback, and failed-payout reservation release without wallet inflation.
- 2026-07-26: Exact Phase 10–12 follow-up completed private payout proof/receipt/timelines; eliminated all route use of the broad admin guard; added structured immutable audit search/export plus login/session evidence and persistent rate limits; and added detailed CRBT, OAC and Content-ID forms, idempotent APIs and provider-level statuses. A regenerated 39-migration baseline, all ten PostgreSQL suites, all domain tests, TypeScript, zero-error lint and the 142-route production build passed. Supplied Razorpay test credentials were not persisted or logged.
- 2026-07-26: The authoritative `npm audit --audit-level=high --omit=dev` gate was requested but rejected because it would transmit dependency names and versions to npm without explicit user authorization. No workaround was attempted. Phase 13 remains active pending that authorization plus production-equivalent staging access for the separately documented Razorpay webhook, durable private storage, provider sandbox, browser/accessibility and operational drills.
- 2026-07-26: Razorpay test API credentials and a distinct test webhook signing secret were configured in ignored, untracked `.env.local`; validation confirmed all three variable names are present exactly once without printing their values. No live callback registration or webhook delivery claim was made.
- 2026-07-26: Corrected the Next.js dynamic-route collision under `/api/admin/releases` by standardizing the DireNote payload-preview route from `[releaseId]` to `[id]`. A clean 142-route production build and TypeScript pass confirmed the generated manifest contains only the canonical `[id]` path.
