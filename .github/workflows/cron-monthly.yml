name: Scheduled jobs (monthly revenue)

on:
  schedule:
    - cron: "55 1 2 * *"
  workflow_dispatch:

concurrency:
  group: cron-monthly

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Call cron endpoints
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          BASE_URL: https://hymnmusic.fun
        run: |
          set -u
          code=$(curl --silent --show-error --max-time 300 \
            -o response.json -w '%{http_code}' \
            -H "Authorization: Bearer $CRON_SECRET" \
            "$BASE_URL/api/cron/direnote-revenue-sync") || code=000
          echo "--- direnote-revenue-sync (GET) HTTP $code"
          head -c 4000 response.json; echo
          [ "$code" = "200" ]
