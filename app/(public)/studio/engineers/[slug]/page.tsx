import Link from "next/link";
import { notFound } from "next/navigation";
import { BadgeCheck, Clock3, Headphones } from "lucide-react";
import { prisma } from "@/lib/prisma";
export const dynamic = "force-dynamic";
function portfolioItems(value: unknown) {
  if (!Array.isArray(value)) return [];
  return value.flatMap(item => {
    if (!item || typeof item !== "object") return [];
    const entry = item as Record<string, unknown>;
    const url = typeof entry.url === "string" ? entry.url : typeof entry.afterUrl === "string" ? entry.afterUrl : null;
    if (!url || !/^https?:\/\//i.test(url)) return [];
    return [{ title: typeof entry.title === "string" ? entry.title : "Portfolio sample", url }];
  });
}
export default async function EngineerPage({ params }: { params: Promise<{ slug: string }> }) { const engineer = await prisma.engineerProfile.findUnique({ where: { slug: (await params).slug }, include: { listings: { where: { active: true, paused: false, serviceType: "MIXING_MASTERING" } }, orders: { where: { review: { is: { status: "PUBLISHED" } } }, include: { review: true }, take: 20, orderBy: { completedAt: "desc" } } } }); if (!engineer || engineer.sellerState !== "ACTIVE") notFound(); const listing=engineer.listings[0]; if(!listing) notFound(); const portfolio=portfolioItems(engineer.portfolio); return <main className="shell pb-24 pt-8"><div className="grid gap-8 lg:grid-cols-[1fr_22rem]"><div><section className="rounded-[2rem] border border-[var(--border)] bg-[var(--card)] p-7 sm:p-10"><div className="flex flex-col gap-5 sm:flex-row sm:items-center"><div className="grid h-24 w-24 place-items-center overflow-hidden rounded-3xl bg-[var(--bg-soft)]">{engineer.profilePhotoUrl?<img src={engineer.profilePhotoUrl} alt="" className="h-full w-full object-cover"/>:<Headphones className="h-9 w-9"/>}</div><div><p className="hymn-kicker">HYMN Studio engineer</p><h1 className="mt-2 flex items-center gap-2 text-4xl font-semibold">{engineer.professionalName}{engineer.verificationState==="VERIFIED"?<BadgeCheck className="h-6 w-6 text-[var(--info)]"/>:null}</h1><p className="mt-2 text-[var(--text-muted)]">{engineer.specialties.join(" · ")}</p></div></div><p className="mt-8 max-w-3xl whitespace-pre-line leading-7 text-[var(--text-muted)]">{engineer.bio}</p></section>{portfolio.length?<section className="mt-6 rounded-[2rem] border border-[var(--border)] bg-[var(--card)] p-7"><h2 className="text-2xl font-semibold">Portfolio</h2><div className="mt-5 grid gap-4">{portfolio.map(item=><div key={item.url} className="rounded-2xl bg-[var(--bg-soft)] p-4"><p className="mb-3 font-medium">{item.title}</p><audio className="w-full" controls preload="metadata" src={item.url}/></div>)}</div></section>:null}<section className="mt-6 rounded-[2rem] border border-[var(--border)] bg-[var(--card)] p-7"><h2 className="text-2xl font-semibold">About this service</h2><p className="mt-4 leading-7 text-[var(--text-muted)]">{listing.description}</p><h3 className="mt-7 font-semibold">Deliverables</h3><pre className="mt-3 whitespace-pre-wrap font-sans text-sm text-[var(--text-muted)]">{JSON.stringify(listing.deliverables,null,2)}</pre></section>{engineer.orders.length?<section className="mt-6 rounded-[2rem] border border-[var(--border)] bg-[var(--card)] p-7"><h2 className="text-2xl font-semibold">Reviews</h2><div className="mt-5 space-y-4">{engineer.orders.map(order=><article key={order.id} className="rounded-2xl bg-[var(--bg-soft)] p-4"><p className="font-medium">{"★".repeat(order.review?.rating??0)}</p>{order.review?.comment?<p className="mt-2 text-sm text-[var(--text-muted)]">{order.review.comment}</p>:null}</article>)}</div></section>:null}</div><aside><div className="sticky top-24 rounded-[2rem] border border-[var(--border-strong)] bg-[var(--card)] p-6 shadow-2xl"><p className="text-sm text-[var(--text-muted)]">Mixing & Mastering</p><p className="mt-2 text-3xl font-semibold text-[var(--money)]">₹{Number(listing.beatCustomerPrice ?? listing.standardPrice).toLocaleString("en-IN")}</p><div className="mt-5 space-y-2 text-sm text-[var(--text-muted)]"><p className="flex items-center gap-2"><Clock3 className="h-4 w-4"/>{listing.turnaroundDays} day delivery</p><p>{listing.includedRevisions} revisions included</p><p>Additional revision: ₹{Number(listing.additionalRevisionPrice).toLocaleString("en-IN")}</p></div><Link className="btn-primary mt-6 w-full justify-center" href={`/studio/start?listing=${listing.publicId}`}>Start project</Link></div></aside></div></main>; }
